import Image from "next/image";
import Link from "next/link";
import {notFound} from "next/navigation";
import {RedHeader} from "../../../../../components/red-header";
import {StudySectionNav} from "../../../../../components/study-section-nav";
import {formulaCategories,formulaCategory,formulaPageLabel,formulaPageNumbers,formulaSubtopic} from "../../../../../lib/formulas";
export function generateStaticParams(){return formulaCategories.flatMap(category=>category.subtopics.map(subtopic=>({category:category.id,subtopic:subtopic.id})))}
export default async function FormulaSubtopicPage({params}:{params:Promise<{category:string;subtopic:string}>}){const{category:categoryId,subtopic:subtopicId}=await params;const category=formulaCategory(categoryId);if(!category)notFound();const subtopic=formulaSubtopic(category,subtopicId);if(!subtopic)notFound();const pages=formulaPageNumbers(subtopic.pages);const sections=pages.map(page=>({label:formulaPageLabel(page),targetId:`formula-page-${page}`}));return <><RedHeader/><main className="digital-textbook"><nav><Link href={`/study/formulas/${category.id}`}>← {category.title}</Link><span>מקור: פורמולות צמחי מרפא • עמ׳ {subtopic.pages}</span></nav><header><span>תת־פרק בפורמולות</span><h1>{subtopic.title}</h1><p>{category.summary}</p><div><b>{category.title}</b><b>עמודים {subtopic.pages}</b></div></header><div className="textbook-layout"><aside><StudySectionNav sections={sections} repeated={["פעילות","אינדיקציות","ניתוח","צמחים","מודיפיקציות","קונטרה אינדיקציות"]}/></aside><article className="source-content"><div className="source-note"><b>חומר המקור המלא</b><p>עמודי ה-PDF מוצגים במלואם ובסדר המקורי, ללא קיצור או שכתוב.</p></div>{pages.map(page=><figure id={`formula-page-${page}`} key={page}><figcaption>{formulaPageLabel(page)} • עמוד {page}</figcaption><Image src={`/formula-pages/page-${String(page).padStart(3,"0")}.jpg`} alt={`${subtopic.title} — עמוד ${page}`} width={1100} height={1556} sizes="(max-width:760px) 100vw, 900px"/></figure>)}</article></div></main></>}
