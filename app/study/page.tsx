import Link from"next/link";import{RedHeader}from"../../components/red-header";import topics from"../../data/study/western-medicine-topics.json";import{allQuestions}from"../../lib/exams";
export default function Study(){return <><RedHeader/><main className="study-page"><div className="section-heading"><span>ספריית רפואה מערבית</span><h1>קטעי לימוד וסיכומים</h1><p>הנושאים מסודרים לפי תוכנית הבחינה. תוכן רפואי יתווסף רק לאחר אימות מקצועי.</p></div><section className="study-topic-grid">{topics.map((t,i)=>{const count=allQuestions.filter(q=>q.topic===t.title).length;return <Link href={`/study/${t.id}`} key={t.id}><span>{String(i+1).padStart(2,"0")}</span><h2>{t.title}</h2><p>{count?`${count} שאלות תרגול קשורות`:`מבנה הסיכום מוכן לתוכן מאומת`}</p><b>פתיחת הנושא ←</b></Link>})}</section></main></>}
