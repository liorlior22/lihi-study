import Link from"next/link";import{RedHeader}from"../../components/red-header";import{StudySearch}from"../../components/study-search";import{allQuestions}from"../../lib/exams";import{sourcePages,studyTopics}from"../../lib/study";
export default function Study(){return <><RedHeader/><main className="study-library"><header><span>ספריית רפואה מערבית</span><h1>קטעי לימוד וסיכומים</h1><p>חומר המקור המלא, מחולק לנושאים וזמין לחיפוש בעברית ובאנגלית.</p></header><StudySearch topics={studyTopics} pages={sourcePages}/><section className="study-topic-grid">{studyTopics.map((topic,i)=>{const count=allQuestions.filter(q=>q.topic===topic.title).length;return <Link href={`/study/${topic.id}`} key={topic.id}><span>{String(i+1).padStart(2,"0")}</span><h2>{topic.title}</h2><p>{topic.summary}</p><small>עמודים {topic.pages} במקור {count?`• ${count} שאלות`:""}</small><b>לפתיחת פרק הלימוד ←</b></Link>})}</section></main></>}
