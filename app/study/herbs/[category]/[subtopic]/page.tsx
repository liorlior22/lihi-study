import Image from "next/image";
import Link from "next/link";
import {notFound} from "next/navigation";
import {RedHeader} from "../../../../../components/red-header";
import {StudySectionNav} from "../../../../../components/study-section-nav";
import {herbCategories,herbCategory,herbPageLabel,herbPageNumbers,herbSubtopic} from "../../../../../lib/herbs";
export function generateStaticParams(){return herbCategories.flatMap(category=>category.subtopics.map(subtopic=>({category:category.id,subtopic:subtopic.id})))}
export default async function HerbSubtopicPage({params}:{params:Promise<{category:string;subtopic:string}>}){const{category:categoryId,subtopic:subtopicId}=await params;const category=herbCategory(categoryId);if(!category)notFound();const subtopic=herbSubtopic(category,subtopicId);if(!subtopic)notFound();const pages=herbPageNumbers(subtopic.pages);const sections=pages.map(page=>({label:herbPageLabel(page),targetId:`herb-page-${page}`}));return <><RedHeader/><main className="digital-textbook"><nav><Link href={`/study/herbs/${category.id}`}>← {category.title}</Link><span>מקור: צמחי מרפא בודדים • עמ׳ {subtopic.pages}</span></nav><header><span>תת־נושא בצמחים</span><h1>{subtopic.title}</h1><p>{category.summary}</p><div><b>{category.title}</b><b>עמודים {subtopic.pages}</b></div></header><div className="textbook-layout"><aside><StudySectionNav sections={sections} repeated={["טעמים וטמפרטורה","איברים","תפקודים","מינון","קונטרה אינדיקציות","שילובים"]}/></aside><article className="source-content"><div className="source-note"><b>חומר המקור המלא</b><p>עמודי ה-PDF מוצגים במלואם ובסדר המקורי, ללא קיצור או שכתוב.</p></div>{pages.map(page=><figure id={`herb-page-${page}`} key={page}><figcaption>עמוד {page} בקובץ צמחים בודדים</figcaption><Image src={`/herb-pages/page-${String(page).padStart(3,"0")}.jpg`} alt={`${subtopic.title} — עמוד ${page}`} width={1100} height={1556} sizes="(max-width:760px) 100vw, 900px"/></figure>)}</article></div></main></>}
