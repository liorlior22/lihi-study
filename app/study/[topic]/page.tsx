import Link from"next/link";import{notFound}from"next/navigation";import{RedHeader}from"../../../components/red-header";import topics from"../../../data/study/western-medicine-topics.json";import{allQuestions}from"../../../lib/exams";
export function generateStaticParams(){return topics.map(t=>({topic:t.id}))}export default async function Topic({params}:{params:Promise<{topic:string}>}){const{topic:id}=await params;const topic=topics.find(t=>t.id===id);if(!topic)notFound();const related=allQuestions.filter(q=>q.topic===topic.title);return <><RedHeader/><main className="topic-page"><Link className="back-link" href="/study">← חזרה לכל הנושאים</Link><div className="section-heading"><span>רפואה מערבית</span><h1>{topic.title}</h1><p>{topic.summary}</p></div><section className="study-sections"><article><h2>סעיפי לימוד</h2><ul>{topic.sections.map(item=><li key={item}>{item}</li>)}</ul></article><article><h2>מושגים חשובים</h2><ul>{topic.terms.map(item=><li key={item}>{item}</li>)}</ul></article><article><h2>נקודות שחוזרות במבחנים</h2><ul>{topic.repeated.map(item=><li key={item}>{item}</li>)}</ul></article><article><h2>מקור החומר</h2><p>רפואה מערבית — מבחן סוף שנה, עמודים {topic.pages}</p><a href="/study-source/western-medicine-final.pdf" target="_blank">פתיחת חומר המקור</a></article></section><section className="related-questions"><h2>שאלות תרגול קשורות</h2>{related.length?<><p>{related.length} שאלות מאומתות זמינות בנושא זה.</p><Link href={`/exams/run?mode=topic&value=${encodeURIComponent(topic.title)}`}>תרגול בנושא</Link></>:<p>אין עדיין שאלות מאומתות בנושא זה. התרגול ייפתח לאחר אימות שאלות מתאימות.</p>}</section></main></>}
