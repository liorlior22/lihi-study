import Link from"next/link";import{notFound}from"next/navigation";import{RedHeader}from"../../../components/red-header";import{allQuestions}from"../../../lib/exams";import{cleanLines,pagesForTopic,studyTopics}from"../../../lib/study";
export function generateStaticParams(){return studyTopics.map(topic=>({topic:topic.id}))}
export default async function Topic({params}:{params:Promise<{topic:string}>}){const{topic:id}=await params;const topic=studyTopics.find(item=>item.id===id);if(!topic)notFound();const pages=pagesForTopic(topic);const related=allQuestions.filter(q=>q.topic===topic.title);return <><RedHeader/><main className="digital-textbook"><nav><Link href="/study">← כל נושאי הלימוד</Link><span>מקור: רפואה מערבית — מבחן סוף שנה • עמ׳ {topic.pages}</span></nav><header><span>פרק לימוד</span><h1>{topic.title}</h1><p>{topic.summary}</p><div>{topic.terms.map(term=><b key={term}>{term}</b>)}</div></header><div className="textbook-layout"><aside><h2>תוכן הפרק</h2>{topic.sections.map(section=><a href="#content" key={section}>{section}</a>)}<h2>חוזר במבחנים</h2>{topic.repeated.map(item=><span key={item}>{item}</span>)}</aside><article id="content" className="source-content"><div className="source-note"><b>חומר המקור המלא</b><p>הטקסט שלהלן חולץ מחומר הלימוד שסופק, ללא קיצור או השלמת מידע רפואי.</p></div>{pages.map(page=><section id={`page-${page.page}`} key={page.page}><div>עמוד {page.page}</div>{cleanLines(page.lines).map((line,i)=><p key={i}>{line}</p>)}</section>)}</article></div><section className="topic-practice"><div><h2>שאלות קשורות</h2><p>{related.length?`${related.length} שאלות מאומתות זמינות בנושא.`:"לא נמצאו עדיין שאלות מאומתות המשויכות ישירות לנושא."}</p></div>{related.length>0&&<Link href={`/exams/run?mode=topic&value=${encodeURIComponent(topic.title)}`}>תרגול בנושא</Link>}</section></main></>}
