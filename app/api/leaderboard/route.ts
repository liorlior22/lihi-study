import{NextResponse}from"next/server";import{databaseReady,playerName,standing,top10}from"../../../lib/leaderboard";
export async function GET(){const name=await playerName();if(!name)return NextResponse.json({name:null,leaders:[],personal:null});if(!databaseReady())return NextResponse.json({name,leaders:[],personal:null,error:"טבלת המצטיינים המשותפת עדיין לא הוגדרה בשרת."},{status:503});try{return NextResponse.json({name,leaders:await top10(),personal:await standing(name)})}catch(error){console.error("Leaderboard database error",error);return NextResponse.json({name,leaders:[],personal:null,error:"החיבור לטבלת המצטיינים המשותפת נכשל. יש לבדוק את הגדרות Supabase ב־Vercel."},{status:503})}}
