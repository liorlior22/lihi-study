(function(){const stats=JSON.parse(localStorage.getItem('english100.stats')||'{}');const mastered=Object.keys(stats.mastered||{}).length;const mistakes=Object.keys(stats.mistakes||{}).length;const level=Math.min(100,Math.floor(mastered/10));const levelScore=document.getElementById('levelScore');const miniLevel=document.getElementById('miniLevel');const levelBar=document.getElementById('levelBar');const vocabProgress=document.getElementById('vocabProgress');const mistakeCount=document.getElementById('mistakeCount');if(levelScore)levelScore.textContent=level;if(miniLevel)miniLevel.textContent=level+'/100';if(levelBar)levelBar.style.width=level+'%';if(vocabProgress)vocabProgress.textContent=mastered+' words mastered';if(mistakeCount)mistakeCount.textContent=mistakes+' mistakes';})();