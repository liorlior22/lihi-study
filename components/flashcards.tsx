"use client";import {useState} from "react";import {Icon} from "./icons";
type Card={topic:string;front:string;back:string;hint:string};
export function Flashcards({cards}:{cards:Card[]}){const [index,setIndex]=useState(0),[flip,setFlip]=useState(false),[known,setKnown]=useState(0);const card=cards[index];function next(got:boolean){if(got)setKnown(v=>v+1);setFlip(false);setIndex(v=>(v+1)%cards.length)}return <><div className="mb-5 flex items-center justify-between"><span className="rounded-full bg-[#e9f6ef] px-3 py-1 text-sm font-bold text-[#27785b]">{card.topic}</span><span className="text-sm text-[#71827c]">כרטיס {index+1} מתוך {cards.length}</span></div><button onClick={()=>setFlip(!flip)} className="soft-grid card flex min-h-[330px] w-full flex-col items-center justify-center p-8 text-center transition hover:border-[#91c5af]"><small className="mb-5 font-bold uppercase tracking-widest text-[#43806b]">{flip?"תשובה":"שאלה"}</small><h2 className={`max-w-xl font-black ${flip?"text-3xl text-[#176b4f]":"text-2xl sm:text-3xl"}`}>{flip?card.back:card.front}</h2>{!flip&&<p className="mt-8 rounded-xl bg-[#f4f8f6] px-4 py-2 text-sm text-[#74847e]">רמז: {card.hint}</p>}<span className="mt-8 flex items-center gap-2 text-xs text-[#8a9994]"><Icon name="cards" size={15}/>לחצי על הכרטיס כדי להפוך אותו</span></button><div className="mt-5 grid grid-cols-2 gap-3"><button onClick={()=>next(false)} className="btn-secondary !border-[#f0d8cf] !text-[#a65c40]">עדיין לא</button><button onClick={()=>next(true)} className="btn-primary"><Icon name="check" size={17}/>ידעתי!</button></div><p className="mt-5 text-center text-sm text-[#768781]">בסבב הזה ידעת <b className="text-[#217459]">{known}</b> כרטיסים</p></>}
