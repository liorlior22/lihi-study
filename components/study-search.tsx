"use client";
import Link from"next/link";import{useMemo,useState}from"react";import type{SourcePage,StudyTopic}from"../lib/study";import{pageNumbers}from"../lib/study";
type Match={topic:StudyTopic;page:number;line:string;context:string};
export function StudySearch({topics,pages}:{topics:StudyTopic[];pages:SourcePage[]}){const[query,setQuery]=useState("");const results=useMemo(()=>{const q=query.trim().toLocaleLowerCase("he");if(q.length<2)return[];const found:Match[]=[];for(const topic of topics){const allowed=new Set(pageNumbers(topic.pages));const metadata=[topic.title,topic.summary,...topic.sections,...topic.terms,...topic.repeated].join(" ").toLocaleLowerCase("he");if(metadata.includes(q)){const first=pageNumbers(topic.pages)[0];found.push({topic,page:first,line:topic.title,context:`${topic.summary} מושגים: ${topic.terms.join(", ")}`})}for(const page of pages.filter(p=>allowed.has(p.page))){page.lines.forEach((line,i)=>{if(line.toLocaleLowerCase("he").includes(q)){const context=page.lines.slice(Math.max(0,i-1),i+2).filter(x=>x!=="o").join(" ");found.push({topic,page:page.page,line,context})}})}}return found},[query,topics,pages]);return <section className="study-search"><label htmlFor="study-query">חיפוש בכל חומר הלימוד</label><div><svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"/><path d="m20 20-4-4"/></svg><input id="study-query" value={query} onChange={e=>setQuery(e.target.value)} placeholder="חיפוש בעברית, English או ראשי תיבות — למשל COPD, Troponin, אנמיה"/><kbd>⌘ K</kbd></div>{query.trim().length>=2&&<aside><header><b>{results.length?`${results.length} תוצאות`:`לא נמצאו תוצאות`}</b><button onClick={()=>setQuery("")}>ניקוי</button></header>{results.map((result,i)=><Link href={`/study/${result.topic.id}#page-${result.page}`} key={`${result.topic.id}-${result.page}-${i}`}><span>{result.topic.title} • עמ׳ {result.page}</span><p>{result.context}</p></Link>)}</aside>}</section>}
