import Link from "next/link";
export function ExamCard({year,count,available}:{year:number;count:number;available:boolean}){return <article className="exam-select-card"><div className="exam-year">{year}</div><h2>מבחן רפואה מערבית</h2><p>{available?`${count} שאלות מאומתות • זמין`:`${count?`${count} מתוך 30 שאלות נקלטו • `:""}ממתין לתשובון מאומת`}</p><div className="exam-card-actions">{available?<Link href={`/exams/run?mode=year&value=${year}`}>התחלת מבחן</Link>:<span>ממתין לתשובון מאומת</span>}<a href={`/exams-pdf/western-${year}.pdf`} target="_blank">צפייה במבחן המקורי</a></div></article>}
